import objects from "../../fixtures/objects.json";
// ***********************************************************
// Use this file to create utltity functions
// that you need during the tests
// and keep steps clean and readable 
// ***********************************************************

const myUnitiltyFunction = function (parameter) {
  
  return parameter;
};

module.exports = {
  myUnitiltyFunction: myUnitiltyFunction,
};